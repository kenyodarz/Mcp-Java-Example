# Examples

## Purpose

This area is reserved for **example-only evidence**.

Examples help explain how the method was applied in real repositories, but they should not become
the default rule set for every new repo.

## Current preparation state

In this staging area, examples are referenced rather than fully copied.

The source example set currently lives in the original repository under:

- `../../../../docs/examples/untitled/README.md`

## Boundary rule

If an example conflicts with the reusable core, prefer the reusable core and treat the example as
contextual evidence.

