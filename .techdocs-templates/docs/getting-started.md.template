# Guía de Inicio (Getting Started)

Esta guía te llevará de **0 a 100** para tener {{PROJECT_NAME}} corriendo en tu máquina local.

## 📋 Prerrequisitos

Antes de empezar, asegúrate de tener instalado:

1. **Java Development Kit (JDK)**: Versión {{JAVA_VERSION}} o superior.
   - *Verificar*: `java -version`
2. **Git**: Para descargar el código.
   - *Verificar*: `git --version`
3. **Claude Desktop** (Opcional): Para probar el servidor.

---

## 🚀 Paso 1: Descargar y Preparar

1. **Clona el repositorio**:
   ```bash
   git clone {{REPO_URL}}
   cd {{PROJECT_NAME}}
   ```

2. **Compila el proyecto**:
   
   En Windows:
   ```powershell
   ./gradlew build
   ```
   
   En Mac/Linux:
   ```bash
   ./gradlew build
   ```

> **✅ Éxito si**: Ves un mensaje que dice `BUILD SUCCESSFUL`.

---

## ⚙️ Paso 2: Configuración (Environment)

Este proyecto necesita algunas variables de entorno para funcionar.

1. Crea una copia del archivo de ejemplo (si existe) o crea un archivo `.env` (o edita tu configuración de Run en IntelliJ).
2. Configura las siguientes variables:

| Variable | Descripción | Ejemplo |
|----------|-------------|---------|
| `SERVER_PORT` | Puerto del servidor | `8080` |
| `LOG_LEVEL` | Nivel de logs | `INFO` |
| `OTRA_VAR` | [Descripción] | `valor` |

---

## ▶️ Paso 3: Ejecutar

Corre el servidor con:

```bash
./gradlew bootRun
```

Deberías ver logs de Spring Boot iniciando. 
Cuando veas: `Tomcat started on port(s): 8080`, ¡estás listo!

---

## 🤖 Paso 4: Conectar a Claude Desktop

Para usar este servidor desde Claude, edita tu archivo de configuración:

- **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`
- **Mac**: `~/Library/Application Support/Claude/claude_desktop_config.json`

Agrega esto dentro de `mcpServers`:

```json
"{{PROJECT_NAME}}": {
  "command": "java",
  "args": [
    "-jar",
    "RUTA_ABSOLUTA_A/{{PROJECT_NAME}}/build/libs/app.jar"
  ]
}
```

> **💡 Tip**: Asegúrate de usar rutas absolutas y barras dobles `\\` en Windows.

---

## 🧪 Paso 5: Probar

1. Reinicia Claude Desktop.
2. Busca el ícono de "enchufe" 🔌. Deberías ver `{{PROJECT_NAME}}` conectado en verde.
3. Pregúntale algo: *"Hola, ¿qué herramientas tienes?"*
